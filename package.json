{
  "name": "runrun",
  "private": true,
  "version": "0.0.0",
  "description": "Static local-first race diary for Elisa Park's Athlinks history",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:python": ".venv/bin/python -m py_compile scripts/build_diary_data.py"
  }
}
