name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Create repo virtualenv
        run: python -m venv .venv

      - name: Install Python dependencies into repo virtualenv
        run: .venv/bin/python -m pip install -r requirements.txt

      - name: Run smoke tests
        run: npm test

      - name: Check diary build script syntax
        run: .venv/bin/python -m py_compile scripts/build_diary_data.py
